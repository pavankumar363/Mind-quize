const roles=document.querySelectorAll('.role');const roleLabel=document.getElementById('roleLabel');const username=document.getElementById('username');roles.forEach(role=>role.addEventListener('click',()=>{roles.forEach(r=>r.classList.remove('active'));role.classList.add('active');const name=role.dataset.role.charAt(0).toUpperCase()+role.dataset.role.slice(1);roleLabel.textContent=name;username.placeholder='Enter '+name.toLowerCase()+' username or ID';}));document.getElementById('loginForm').addEventListener('submit',e=>{e.preventDefault();const role=document.querySelector('.role.active').dataset.role;const note=document.getElementById('demoNote');note.textContent='Demo login selected for '+role+'. Dashboard connection will be added in the next phase.';note.style.color='#7bea8b';});